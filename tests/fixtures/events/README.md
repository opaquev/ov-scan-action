# Event Payload Fixtures

PR #6 populates this directory with full GitHub event payload JSON
fixtures (same-repo-pr.json, fork-pr.json, push.json, etc.). For PR #4
(red phase) tests synthesize event payloads inline via `make_event_payload`
in `tests/helpers.bash`, so this README is the only file present.
