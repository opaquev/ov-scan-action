; Fake Stripe-style secret-test key for fixture use.
; Prefixed with XXXFAKE_ so it does NOT match Stripe's real sk_test_ regex
; and does NOT trip push protection. See dirty-repo/README.md.

[stripe]
publishable_key = pk_test_PLACEHOLDERPLACEHOLDERPLACEHOLDER
secret_key      = XXXFAKE_sk_test_FAKEFAKEFAKEFAKEFAKEFAKE
webhook_secret  = whsec_PLACEHOLDERPLACEHOLDERPLACEHOLDER
